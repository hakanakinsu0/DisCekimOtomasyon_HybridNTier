@model PackageOptionCreatePageVm

@{
    ViewData["Title"] = "Yeni Paket Seçeneği Ekle";
}

<div class="container py-4">
    <h2>@ViewData["Title"]</h2>

    <form asp-action="Create" method="post" novalidate>
        @Html.AntiForgeryToken()
        <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

        <div class="form-floating mb-3">
            <input asp-for="Request.Name" class="form-control" placeholder="Paket Adı" />
            <label asp-for="Request.Name"></label>
            <span asp-validation-for="Request.Name" class="text-danger small"></span>
        </div>

        <div class="form-floating mb-4">
            <input asp-for="Request.Price" class="form-control" placeholder="Fiyat" />
            <label asp-for="Request.Price"></label>
            <span asp-validation-for="Request.Price" class="text-danger small"></span>
        </div>

        <button type="submit" class="btn btn-success w-100">Kaydet</button>
    </form>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
